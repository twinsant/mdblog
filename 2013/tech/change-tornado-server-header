# Change Tornado responsed Server header

Show me the code:

    class BaseHandler(RequestHandler):
        def set_default_headers(self):
            self.set_header('Server', 'Stay hungry, Stay foolish')

That's all.
